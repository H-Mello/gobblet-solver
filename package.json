{
  "name": "gobblet-solver",
  "version": "0.0.0",
  "description": "Solver and static web UI for a 3x3 Gobblet-Jr-like tic-tac-toe variant.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev:web": "vite",
    "build:web": "vite build"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^1.6.0",
    "vite": "^6.0.0"
  }
}
