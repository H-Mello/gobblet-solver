{
  "name": "gobblet-solver",
  "version": "0.0.0",
  "description": "Simulator and solver for a modified tic-tac-toe (Gobblet-Jr-style) game.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  }
}
